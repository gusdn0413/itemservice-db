spring:
  profiles:
    active: test
  datasource:
    generate-unique-name: false
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/testcase
#    username: sa
#    password:

logging:
  level:
    org:
      springframework:
        jdbc: debug
